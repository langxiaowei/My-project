<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.notification.mapper.NotificationMapper">

    <insert id="insert" parameterType="com.example.notification.pojo.NotificationRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_notification
        (alarm_id, device_id, alarm_type, level, content, status, create_time)
        VALUES
        (#{alarmId}, #{deviceId}, #{alarmType}, #{level}, #{content}, #{status}, NOW())
    </insert>

</mapper>